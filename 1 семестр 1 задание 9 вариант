#include <bits/stdc++.h>
#include <iomanip>
#include <cmath>
using namespace std;

int main() 
{
    float x;
    cout << "Введите значение x:" << endl;
    cin >> x;


    float result = abs(pow(x, 3) - pow(x, 2) - 7 * x) / (pow(x, 3) - 15 * x);

    cout << "Результат = " << fixed << setprecision(4) << result << endl;

    return 0;
}